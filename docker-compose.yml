version: '3'

services:
  ssl-api:
    container_name: ssl-api
    image: dv4all/ssl-api-express:0.0.3
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env/ssl-api.env
    ports:
      - "8433:8433"
