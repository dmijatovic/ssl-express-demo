version: '3'

services:
  ssl-api:
    container_name: ssl-api
    image: dv4all/ssl-api:0.0.1
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./env/ssl-api.env
    ports:
      - "8081:8081"
